import Link from "next/link"
import { getAllPosts } from "@/lib/blog"

export default function BlogPage() {
  const posts = getAllPosts()

  return (
    <div className="min-h-screen bg-black text-white font-mono">
      <div className="container mx-auto px-4 py-16">
        <div className="max-w-2xl mx-auto text-center space-y-8">
          {posts.length === 0 ? (
            <div className="space-y-4">
              <h1 className="text-xs uppercase tracking-wider">NO POSTS FOUND</h1>
              <p className="text-xs text-gray-400">Add .md files to the /content folder</p>
            </div>
          ) : (
            posts.map((post) => (
              <Link key={post.slug} href={`/blog/${post.slug}`} className="block hover:text-gray-300 transition-colors">
                <div className="space-y-2">
                  <h2 className="text-xs uppercase tracking-wider">{post.title}</h2>
                  <p className="text-xs text-gray-400">{post.excerpt}</p>
                  <div className="flex justify-center items-center gap-4 text-xs text-gray-500">
                    <span>{post.date}</span>
                    <span>•</span>
                    <span>{post.readTime}</span>
                  </div>
                </div>
              </Link>
            ))
          )}
        </div>
      </div>
    </div>
  )
}
